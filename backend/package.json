{
  "name": "backend2",
  "scripts": {
     "seed": "tsc -p tsconfig.seed.json && node dist/prisma/seed.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "@prisma/extension-accelerate": "^1.2.1",
    "@priyanshu082/common": "^1.0.1",
    "hono": "^4.6.10",
    "prisma": "^5.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "ts-node": "^10.9.2",
    "wrangler": "^3.83.0"
  }
}
